<%= form_with(model: [ current_user, current_user.articles.build ] , local: true) do |form| %>
  
  <% if current_user.articles.build.errors.any? %>
    <div id="error_explanation", class="has-text-black">
      <h2><%= pluralize(current_user.articles.build.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% current_user.articles.build.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= form.text_field :title, class: "input", placeholder: "Tytuł artykułu"%>
  </div>

  <div class="field">
    <%= form.text_area :text, class: "textarea is-link", placeholder: "Treść artykułu..."  %>
  </div>

  <div class="actions">
    <%= form.submit "Opublikuj", class: "button" %>
  </div>
<% end %>
